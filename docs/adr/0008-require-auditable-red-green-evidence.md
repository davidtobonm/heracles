# Require auditable Red and Green Evidence

Heracles will require the Implementer to record Red Evidence before implementation and Green Evidence after the smallest correct change. The Reviewer will inspect both artifacts and review the result for unnecessary scope, abstractions, and duplication. Missing evidence blocks delivery unless the issue carries a reasoned `heracles:tdd-exempt` label.
