# Compose Labors from independent stages

Heracles will expose planning, issue creation, and implementation as independently runnable stages. A Labor composes those stages into the full workflow and pauses at approval gates before publishing issues, preserving human control over product scope while keeping the workflow connected.
