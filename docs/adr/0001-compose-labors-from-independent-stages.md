# Compose Labors from independent stages

Heracles will expose planning, issue creation, and implementation as independently runnable stages. A Labor composes those stages into the full workflow. The Planning Stage requires human approval of its PRD Issue; after approval, issue publication proceeds in the background without a separate Approval Gate, and the Implementation Stage continues against the resulting Defined Backlog.
