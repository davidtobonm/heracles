# Support GitHub issues in v1

Heracles v1 will use GitHub Issues as its only issue tracker. GitHub-specific behavior will sit behind an internal tracker boundary, and the MCP server will expose Heracles workflow operations rather than raw GitHub commands, keeping future tracker support possible without expanding the initial product.
